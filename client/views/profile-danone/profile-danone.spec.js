'use strict';

describe('Controller: ProfileDanoneCtrl', function () {

  beforeEach(module('gravity'));

  var ProfileDanoneCtrl;

  beforeEach(inject(function ($controller) {
    ProfileDanoneCtrl = $controller('ProfileDanoneCtrl', {});
  }));

  it('should ...', function () {
    expect(1).toBe(1);
  });

});
