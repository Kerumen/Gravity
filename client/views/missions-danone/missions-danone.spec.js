'use strict';

describe('Controller: MissionsDanoneCtrl', function () {

  beforeEach(module('gravity'));

  var MissionsDanoneCtrl;

  beforeEach(inject(function ($controller) {
    MissionsDanoneCtrl = $controller('MissionsDanoneCtrl', {});
  }));

  it('should ...', function () {
    expect(1).toBe(1);
  });

});
